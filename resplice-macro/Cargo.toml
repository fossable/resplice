[package]
name = "resplice-macro"
version = "0.0.1"
description = "Rewrite it in Rust"
edition = "2024"
license = "Unlicense"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
